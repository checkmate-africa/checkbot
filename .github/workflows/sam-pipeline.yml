on:
  pull_request:
    branches:
      - '**'
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          echo hello ${{ secrets.AWS_REGION }}
